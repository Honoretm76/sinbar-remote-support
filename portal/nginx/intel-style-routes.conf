# The vendor-signed portable portal no longer uses a custom-protocol session
# API. The reviewed production Nginx configuration, including all four exact
# download routes and the default-deny rule, is:
#
#   deploy/nginx.conf
#
# Keep one canonical configuration so the customer download filename token and
# pinned vendor artifact paths cannot drift between duplicated snippets.
